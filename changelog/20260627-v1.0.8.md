# 1.0.8 - First Successful Sync

Status: prepared in codebase

- reworked Setup into a first-sync checklist covering Google APIs, exact redirect URI copy, OAuth JSON/manual credentials, account connection, and the first synced draft path
- added connected-state Create synced draft links to the existing Posts list Add Sync Doc flow
- improved Sources, Logs, and Drive browser empty states with next actions and decorative illustrations
- added a POT template, repeatable i18n scripts, and `wp_set_script_translations()` wiring for admin bundles
- translated OAuth JSON parser errors and clarified redirect URI/test-user/Drive read-only setup copy
- bumped plugin, readme, and package metadata to `1.0.8`

Unresolved:

- WordPress.org screenshot slot 6 still needs a real Sources-table capture from local/staging WordPress admin
- final manual Google OAuth and first-sync QA still needs a configured WordPress site and connected Google test account
