# 1.0.9 - Security Hardening

Status: prepared in codebase

- added shared DocSync use permission: administrators, or users with edit/create capability on at least one enabled target post type
- required `X-WP-Nonce` for mutating DocSync REST requests; kept `_wpnonce` only for read-only `GET`
- tightened REST validation for Drive list page sizes, source/log pagination, source status/log filters, source payloads, post IDs, sync mode, file IDs, and settings fields
- limited post/list-table DocSync assets, missing-build notices, and source metadata output to capable users
- added defensive sync diagnostic redaction for token-like values
- bumped plugin, readme, package, admin fallback config, and POT metadata to `1.0.9`

Unresolved:

- staging role matrix still needed before tag
- final Google OAuth connect and first-sync smoke QA still needed before tag
- WordPress.org screenshot slot 6 from `1.0.8` remains open
