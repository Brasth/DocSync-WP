# 1.1.0 - Layout Preset Foundation

- added default synced layout presets for Gutenberg sync: Clean Article, Documentation, and Plain Blocks
- added the Setup sync defaults dropdown and REST settings fields for `defaultLayoutPreset` and `availableLayoutPresets`
- preserved upgraded installs on Plain Blocks while seeding new installs with Clean Article
- added layout fingerprint metadata so preset changes force safe re-conversion
- kept Elementor sync behavior unchanged
- added golden layout fixtures and a local verifier script

Unresolved:

- manual staging sync still needs a configured WordPress site and connected Google test account
